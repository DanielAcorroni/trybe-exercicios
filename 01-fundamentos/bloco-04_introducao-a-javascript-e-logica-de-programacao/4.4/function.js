function sum(primeiroNumero, segundoNumero) {
    
    let adicao = primeiroNumero + segundoNumero;

    return adicao;
}

console.log(sum(50, 30));

console.log("");

function division(firstNumber, firstNumber) {

    let divisao = firstNumber / firstNumber;

    return divisao;
}

console.log(division(100, 10));

console.log("");

function impar(firstNumber, firstNumber, terceiroNumero) {

    if (firstNumber % 2 === 1 || firstNumber % 2 === 1 || terceiroNumero % 2 === 1) {
        return true;
    }
    
    else {
        return false;
    }

}

console.log(impar(26, 5, 20));

console.log("");

function biggerOf2Numbers(firstNumber, secondNumber) {

    if (firstNumber > secondNumber) {
        return firstNumber;
    }
    else {
        return secondNumber;
    }

}

console.log(biggerOf2Numbers(500, 10));

console.log("");

